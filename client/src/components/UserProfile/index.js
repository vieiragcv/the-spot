import React, { useState } from 'react';
/* import { Link } from 'react-router-dom'; */




/*------------------------------------------------------------
-          COMPONENT: USER PROFILE
------------------------------------------------------------*/

const UserProfile = ( ) => {

/*     if (!username) {
    return <p className=''> {username} , Username not found !</p>
  } */
    
  return (
    <div className=''>
{/*       {loggedIn && (

      )}
          <h5>{username}</h5> */}
    </div>
  );
};

export default UserProfile;